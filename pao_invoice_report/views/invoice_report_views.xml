<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_order_invoice_tree" model="ir.ui.view">
        <field name="name">invoice.report.tree</field>
        <field name="model">invoice.report</field>
        <field name="arch" type="xml">
            <tree>
                <field name="invoice_id" widget="many2one"/>
                <field name="client_id" optional="hide"/>
                <field name="customer"/>
                <field name="group"/>
                <field name="auditor"/>
                <field name="service_start_date"/>
                <field name="service_end_date"/>
                <field name="promoter"/>
                <field name="product"/>
                <field name="product_category"/>
                <field name="quantity"/>
                <field name="currency_id"/>
                <field name="currency_rate"/>
                <field name="price_subtotal"/>
                <field name="company_currency_subtotal" invisible="not company_currency_subtotal"/>
                <field name="usd_exchange_rate" invisible="not usd_exchange_rate" optional="hide"/>
                <field name="usd_subtotal" invisible="not usd_subtotal" optional="hide"/>
                <field name="eur_exchange_rate" invisible="not eur_exchange_rate" optional="hide"/>
                <field name="eur_subtotal" invisible="not eur_subtotal" optional="hide"/>
                <field name="tax_amount" optional="hide"/>
                <field name="company_currency_tax" invisible="not company_currency_tax" optional="hide"/>
                <field name="invoice_partner_name"/>
                <field name="fiscal_folio"/>
                <field name="invoice_date"/>
                <field name="invoice_date_due"/>
                <field name="invoice_salesperson"/>
                <field name="sales_team"/>
                <!-- <field name="category_id"/> -->
                <field name="scheme_id"/>
                <field name="origin"/>
                <field name="quotation_salesperson"/>
                <!-- <field name="amount_untaxed"/>
                <field name="amount_total"/> -->
                <field name="state"/>
                <field name="payment_state"/>
                <!-- <field name="usd_net"/>
                <field name="usd_total"/>
                <field name="mxn_net"/>
                <field name="mxn_total"/> -->
            </tree>
        </field>
    </record>

    <record id="pao_action_invoice_report" model="ir.actions.act_window">
        <field name="name">Invoice Report</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">invoice.report</field>
        <field name="view_mode">tree</field>
        <!-- <field name="view_id"></field> -->
        <!-- <field name="help">This report performs analysis on your Service Agreements sales orders. Check your Sales Agreements and sort it by different group criteria (coordinator, organization, service start date, service end date, etc.).</field> -->
    </record>

</odoo>