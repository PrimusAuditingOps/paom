<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_pao_site_plot_globalgap_import_wizard_form" model="ir.ui.view">
        <field name="name">pao.site.plot.globalgap.import.wizard.form</field>
        <field name="model">pao.site.plot.globalgap.import.wizard</field>
        <field name="arch" type="xml">
            <form string="Importar sitios (GLOBALG.A.P.)">
                <field name="sale_order_id" invisible="1"/>
                <field name="state" invisible="1"/>
                <group invisible="state != 'draft'">
                    <div class="text-muted mb-2">
                        Formato "Estimación de duración de auditorías GLOBALG.A.P." — la tabla
                        de sitios (Nombre del sitio, Cultivo, Zona, Superficie, Coordenadas,
                        Distancia PHU) puede estar debajo de un bloque de datos del cliente;
                        el sistema la localiza automáticamente.
                    </div>
                    <field name="import_file" filename="file_name"/>
                    <field name="file_name" invisible="1"/>
                </group>
                <group string="Vista previa" invisible="state == 'draft'">
                    <field name="total_rows"/>
                    <field name="valid_rows"/>
                    <field name="error_rows"/>
                    <field name="preview_html" nolabel="1"/>
                </group>
                <footer>
                    <button name="action_preview" type="object" string="Vista previa"
                            class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_import" type="object" string="Importar"
                            class="btn-primary" invisible="state != 'preview'"/>
                    <button string="Cerrar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_pao_site_plot_globalgap_import_wizard" model="ir.actions.act_window">
        <field name="name">Importar sitios (GLOBALG.A.P.)</field>
        <field name="res_model">pao.site.plot.globalgap.import.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
